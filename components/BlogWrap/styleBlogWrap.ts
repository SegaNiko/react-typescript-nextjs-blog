import styled from "styled-components";

const BlogWrapStyle = styled.div`
  margin: 0 auto;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
  max-width: 1100px;
`;

export default BlogWrapStyle;
